ext {
    kmkVersionCode = 0
    extName = 'AniZone'
    extClass = '.AniZone'
    extVersionCode = 5
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib:playlist-utils"))
}
